# Repair protocol

Repair is a bounded sequence of typed operations against an explicit QA report. It must declare affected parts, preservation constraints, maximum attempts, and rollback-on-failure. AI suggestions never bypass deterministic QA.
