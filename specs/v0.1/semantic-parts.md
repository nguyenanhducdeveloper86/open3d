# Semantic parts

`part_id` is the stable identity used by edits, QA, GLB node extras, and future engine adapters. IDs are opaque to geometry code, case-sensitive, and must not be recycled for a different meaning.
