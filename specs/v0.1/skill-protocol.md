# Skill protocol

Future skills must declare typed input/output schemas, runtime/version, permissions, resource limits, validators, license, and provenance. A skill without tests and license metadata cannot receive a production badge.

The current core does not execute skill packages. Blender/community workers must be added as a constrained process with network denied by default.
