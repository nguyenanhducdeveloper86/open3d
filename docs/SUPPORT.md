# Support

Use GitHub issues for reproducible core bugs and feature proposals. Include Open3D version, Python version, operating system, command, contract fixture, and the relevant artifact/checkpoint IDs. Never attach secrets or private assets.

Provider, Blender, Unity, and GPU failures should include the adapter version and upstream runtime version; the core project cannot reproduce an unpinned external environment.
